[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fast-scene"
version = "0.0.1"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["fast_scene"]

[tool.setuptools.package-dir]
fast_scene = "."

